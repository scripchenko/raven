<UserControl x:Class="UnifiedMessenger.App.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:UnifiedMessenger.App.Services.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             AutomationProperties.AutomationId="WelcomeView"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="960">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="48,72">
            <Border MaxWidth="680"
                    Padding="44"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource DividerBrush}"
                    BorderThickness="1"
                    CornerRadius="24">
                <StackPanel>
                    <TextBlock Margin="0,0,0,12"
                               HorizontalAlignment="Center"
                               FontSize="26"
                               FontWeight="SemiBold"
                               Text="{loc:Text Key='raven — messaging and email in one place.'}" />
                    <TextBlock MaxWidth="620"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="{loc:Text Key='To get started, open Settings and add the accounts you need.'}"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />

                    <Button Margin="0,18,0,0"
                            HorizontalAlignment="Center"
                            Content="{loc:Text Key='Support: @dscripchenko'}"
                            Click="Support_Click"
                            Padding="16,8" />

                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
