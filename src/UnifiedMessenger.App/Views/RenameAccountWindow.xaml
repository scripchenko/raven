<Window x:Class="UnifiedMessenger.App.Views.RenameAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:UnifiedMessenger.App.Services.Localization"
        Title="{loc:Text Key='Rename account'}"
        Width="430"
        Height="235"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="22" FontWeight="SemiBold" Text="{loc:Text Key='Rename account'}" />

        <StackPanel Grid.Row="1" Margin="0,22,0,0">
            <TextBlock Margin="0,0,0,7" FontWeight="SemiBold" Text="{loc:Text Key='Account name'}" />
            <TextBox x:Name="AccountNameTextBox"
                     Height="40"
                     Padding="10,7"
                     MaxLength="80"
                     VerticalContentAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,22,0,0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
            <Button Width="96" Height="40" Margin="0,0,10,0" Click="Cancel_Click" Content="{loc:Text Key='Cancel'}" IsCancel="True" />
            <Button Width="112" Height="40" Click="Save_Click" Content="{loc:Text Key='Save'}" IsDefault="True" Style="{StaticResource PrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
