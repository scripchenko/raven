<Window x:Class="UnifiedMessenger.App.Views.ChangeMailAppPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Изменить пароль приложения"
        Width="500"
        Height="340"
        MinWidth="460"
        MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="23"
                       FontWeight="SemiBold"
                       Text="Изменить пароль приложения" />
            <TextBlock Margin="0,8,0,0"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="{Binding PasswordGuidance}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,20,0,18">
            <TextBlock FontWeight="SemiBold" Text="Аккаунт" />
            <TextBlock Margin="0,6,0,0"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="{Binding EmailAddress}" />
            <TextBlock Margin="0,18,0,0"
                       FontWeight="SemiBold"
                       Text="Новый пароль приложения" />
            <PasswordBox x:Name="PasswordBox"
                         Margin="0,7,0,0"
                         AutomationProperties.Name="Новый пароль приложения" />
            <TextBlock x:Name="StatusText"
                       Margin="0,12,0,0"
                       Foreground="#B3261E"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="105"
                    Click="Cancel_Click"
                    Content="Отмена"
                    IsCancel="True" />
            <Button x:Name="SaveButton"
                    Width="180"
                    Margin="10,0,0,0"
                    Click="Save_Click"
                    Content="Проверить и сохранить"
                    IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
