<Window x:Class="UnifiedMessenger.App.Views.StartupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:UnifiedMessenger.App.Services.Localization"
        Title=""
        Width="430"
        Height="260"
        MinWidth="430"
        MinHeight="260"
        MaxWidth="430"
        MaxHeight="260"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="False"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource DividerBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Background="Transparent"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" />
                <Button Grid.Column="1"
                        Width="32"
                        Height="28"
                        Margin="0,4,4,4"
                        Padding="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="Exit_Click"
                        Content="×"
                        FontSize="20"
                        ToolTip="{loc:Text Key='Exit'}" />
            </Grid>

            <Border Grid.Row="1" Padding="32,8,32,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Image Width="194"
                           Height="50"
                           HorizontalAlignment="Center"
                           Source="/UnifiedMessenger.App;component/Assets/Branding/raven_logo.png"
                           Stretch="Uniform"
                           SnapsToDevicePixels="True"
                           UseLayoutRounding="True" />
                    <TextBlock Grid.Row="1"
                               Margin="0,10,0,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="{loc:Text Key='Connecting messengers…'}" />
                    <TextBlock x:Name="ServiceNameText"
                               Grid.Row="2"
                               Margin="0,10,0,0"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Text="{loc:Text Key='Preparing…'}" />
                    <ProgressBar x:Name="StartupProgressBar"
                                 Grid.Row="3"
                                 Height="7"
                                 Margin="0,10,0,0"
                                 HorizontalAlignment="Stretch"
                                 Minimum="0"
                                 Maximum="1">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Grid x:Name="PART_Track" ClipToBounds="True">
                                    <Border Background="#FFE9EEF4" CornerRadius="3.5" />
                                    <Border x:Name="PART_Indicator"
                                            HorizontalAlignment="Left"
                                            CornerRadius="3.5">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Offset="0" Color="#FF20D7EE" />
                                                <GradientStop Offset="1" Color="#FF2D7DFF" />
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <TextBlock x:Name="ProgressText"
                               Grid.Row="4"
                               Margin="0,12,0,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="{loc:Text Key='0 of 0'}" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
