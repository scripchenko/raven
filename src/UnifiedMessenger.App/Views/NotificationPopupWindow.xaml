<Window x:Class="UnifiedMessenger.App.Views.NotificationPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="380"
        MaxHeight="240"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        Topmost="True">
    <Border Margin="6"
            Padding="16"
            Background="#F92A2D33"
            BorderBrush="#4DFFFFFF"
            BorderThickness="1"
            CornerRadius="12"
            MouseLeftButtonUp="Popup_MouseLeftButtonUp">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" Opacity="0.35" ShadowDepth="4" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="0,0,12,8"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="#FF71B7FF"
                       Text="{Binding ServiceName}" />
            <Button Grid.Row="0"
                    Grid.Column="1"
                    Width="28"
                    Height="28"
                    Margin="0,-7,-7,0"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Content="×"
                    Focusable="False"
                    FontSize="20"
                    Foreground="White"
                    ToolTip="Закрыть"
                    Click="CloseButton_Click" />

            <TextBlock Grid.Row="1"
                       Grid.ColumnSpan="2"
                       Margin="0,0,0,6"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="White"
                       Text="{Binding Title}"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="Wrap" />
            <TextBlock Grid.Row="2"
                       Grid.ColumnSpan="2"
                       MaxHeight="92"
                       FontSize="13"
                       Foreground="#FFD7DCE3"
                       Text="{Binding Body}"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="Wrap" />
        </Grid>
    </Border>
</Window>
